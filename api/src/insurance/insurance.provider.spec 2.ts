import { INSURANCE_PROVIDER } from './insurance.provider';

describe('insurance.provider', () => {
  it('exports INSURANCE_PROVIDER token', () => {
    expect(INSURANCE_PROVIDER).toBe('INSURANCE_PROVIDER');
  });
});
